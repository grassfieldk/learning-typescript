// ---- 04: 配列 ----

// (1) numbers の各要素を 2 倍にした新しい配列を map で作り、出力せよ
const numbers = [1, 2, 3, 4, 5];
const doubled = ; // この行を編集
console.log(doubled);

// (2) numbers から偶数だけを取り出した新しい配列を filter で作り、出力せよ
const evens = ; // この行を編集
console.log(evens);

// (3) numbers のすべての要素の合計を reduce で求め、出力せよ
const total = ; // この行を編集
console.log(total);

// (4) words をスプレッド構文で結合し、新しい配列 all を作り出力せよ
const words1 = ["foo", "bar"];
const words2 = ["baz", "qux"];
const all = ; // この行を編集
console.log(all);

// (5) toReversed() を使って all を逆順にした配列を reversed に代入し、出力せよ
//     all も出力し、元の順序が変わっていないことを確認せよ
const reversed = ; // この行を編集
console.log(reversed);
console.log(all);
