// ---- 04: 配列 ----

// (1) numbers の各要素を 2 倍にした新しい配列を map で作り、コンソールに出力せよ
const numbers = [1, 2, 3, 4, 5];
const doubled = ;
console.log(doubled);

// (2) numbers から偶数だけを取り出した新しい配列を filter で作り、コンソールに出力せよ
const evens = ;
console.log(evens);

// (3) numbers のすべての要素の合計を reduce で求め、コンソールに出力せよ
const total = ;
console.log(total);

// (4) words をスプレッド構文で結合し、新しい配列 all を作りコンソールに出力せよ
const words1 = ["foo", "bar"];
const words2 = ["baz", "qux"];
const all = ;
console.log(all);

// (5) all を逆順にした配列を reversed に代入し、コンソールに出力せよ
//     ただし all 自体は変更しないこと
const reversed = ;
console.log(reversed);
console.log(all);
